{
  "name": "hot-takes-api",
  "version": "1.0.0",
  "description": "rest api write for a web cook sharing application",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "t. savigner",
  "license": "ISC",
  "dependencies": {
    "mongoose": "^6.1.2",
    "mongoose-unique-validator": "^3.0.0"
  },
  "devDependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.15"
  }
}
